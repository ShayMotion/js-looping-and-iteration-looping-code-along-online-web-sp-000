// Code your solutions in this file

function writeCards() { 
  for (let i = 0, i < cards.length; i++) {
    console.log("Thank you, ${cards[i], for the wondeful birthday gift!");
  }
  
  return cards;
}

writeCards(cards);